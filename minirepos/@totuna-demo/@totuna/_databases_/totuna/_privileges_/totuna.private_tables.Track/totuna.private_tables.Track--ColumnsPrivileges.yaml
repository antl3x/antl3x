kind: ColumnsPrivileges
metadata:
  name: totuna.private_tables.Track
spec:
  database: totuna
  schema: private_tables
  table: Track
  privileges:
    - {column: AlbumId, privileges: [{role: postgres, privileges: [UPDATE, SELECT, REFERENCES, INSERT]}]}
    - {column: Bytes, privileges: [{role: postgres, privileges: [UPDATE, INSERT, SELECT, REFERENCES]}]}
    - {column: Composer, privileges: [{role: postgres, privileges: [INSERT, REFERENCES, UPDATE, SELECT]}]}
    - {column: GenreId, privileges: [{role: postgres, privileges: [REFERENCES, INSERT, UPDATE, SELECT]}]}
    - {column: MediaTypeId, privileges: [{role: postgres, privileges: [UPDATE, SELECT, INSERT, REFERENCES]}]}
    - {column: Milliseconds, privileges: [{role: postgres, privileges: [SELECT, UPDATE, INSERT, REFERENCES]}]}
    - {column: Name, privileges: [{role: postgres, privileges: [INSERT, UPDATE, REFERENCES, SELECT]}]}
    - {column: TrackId, privileges: [{role: postgres, privileges: [REFERENCES, INSERT, UPDATE, SELECT]}]}
    - {column: UnitPrice, privileges: [{role: postgres, privileges: [SELECT, INSERT, REFERENCES, UPDATE]}]}
