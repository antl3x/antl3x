kind: TablePrivileges
metadata:
  name: totuna.private_tables.documents
spec:
  database: totuna
  schema: private_tables
  table: documents
  privileges:
    - {
        role: postgres,
        privileges: [INSERT, SELECT, UPDATE, DELETE, TRUNCATE, TRIGGER],
      }
