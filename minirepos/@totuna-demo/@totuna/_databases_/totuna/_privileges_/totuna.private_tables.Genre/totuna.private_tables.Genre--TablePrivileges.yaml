kind: TablePrivileges
metadata:
  name: totuna.private_tables.Genre
spec:
  database: totuna
  schema: private_tables
  table: Genre
  privileges:
    - {role: postgres, privileges: [REFERENCES, TRUNCATE, TRIGGER]}
