kind: ViewPrivileges
metadata:
  name: totuna.public_views.customers
spec:
  database: totuna
  schema: public_views
  view: customers
  privileges:
    - {role: postgres, privileges: [INSERT, SELECT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER]}
