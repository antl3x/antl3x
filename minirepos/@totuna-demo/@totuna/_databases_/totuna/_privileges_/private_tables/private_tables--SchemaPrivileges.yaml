kind: SchemaPrivileges
metadata:
  name: private_tables
spec:
  database: totuna
  schema: private_tables
  privileges:
    - {role: PUBLIC, privileges: [USAGE, CREATE]}
    - {role: customer:xyz, privileges: [USAGE]}
    - {role: postgres, privileges: [CREATE, USAGE]}
