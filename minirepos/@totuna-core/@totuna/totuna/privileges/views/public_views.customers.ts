export const privileges = {
  postgres: ["SELECT", "INSERT", "UPDATE", "DELETE", "TRUNCATE", "REFERENCES", "TRIGGER"],
};

export const meta = {
  schema: "public_views",
  view: "customers",
  database: "totuna",
};
