kind: SchemaPrivileges
metadata:
  name: totuna_public_views_privileges
spec:
  database: totuna
  schema: public_views
  rolePrivileges:
    postgres:
      - USAGE
      - CREATE
    customer:xyz:
      - USAGE
      - CREATE
      - INSERT
