kind: SequencePrivileges
metadata:
  database: totuna
  schema: private_tables
  sequence: seq
spec:
  - role: PUBLIC
    privileges: [USAGE, SELECT, UPDATE]

  - role: authenticated
    privileges: [UPDATE, SELECT, USAGE]

  - role: postgres
    privileges: [SELECT, USAGE, UPDATE]
