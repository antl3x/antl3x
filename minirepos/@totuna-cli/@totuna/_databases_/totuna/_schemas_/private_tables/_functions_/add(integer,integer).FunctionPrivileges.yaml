kind: FunctionPrivileges
metadata:
  database: totuna
  schema: private_tables
  function: add(integer,integer)
spec:
  - role: postgres
    privileges:
      - EXECUTE
  - role: authenticated
    privileges:
      - EXECUTE
