kind: TablePrivileges
metadata:
  name: totuna.private_tables.Customer
spec:
  database: totuna
  schema: private_tables
  table: Customer
  privileges:
    - role: PUBLIC
      privileges:
        - UPDATE
        - SELECT
        - INSERT
        - TRIGGER
        - REFERENCES
        - TRUNCATE
        - DELETE
    - role: postgres
      privileges:
        - UPDATE
        - INSERT
        - SELECT
        - TRIGGER
        - REFERENCES
        - TRUNCATE
        - DELETE
