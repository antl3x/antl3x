kind: ColumnsPrivileges
metadata:
  name: totuna.private_tables.Employee
spec:
  database: totuna
  schema: private_tables
  table: Employee
  privileges:
    - column: Address
      privileges:
        - role: outra
          privileges:
            - SELECT
    - column: EmployeeId
      privileges:
        - role: outra
          privileges:
            - SELECT
