kind: SequencePrivileges
metadata:
  name: totuna.private_tables.documents_id_seq
spec:
  database: totuna
  schema: private_tables
  sequence: documents_id_seq
  privileges:
    - role: PUBLIC
      privileges: []
