kind: TablePrivileges
metadata:
  name: totuna.private_tables.Artist
spec:
  database: totuna
  schema: private_tables
  table: Artist
  privileges:
    - role: postgres
      privileges:
        - SELECT
        - INSERT
        - TRIGGER
        - REFERENCES
        - TRUNCATE
