kind: TablePrivileges
metadata:
  name: totuna.private_tables.Invoice
spec:
  database: totuna
  schema: private_tables
  table: Invoice
  privileges:
    - role: postgres
      privileges:
        - DELETE
        - UPDATE
        - SELECT
        - INSERT
        - REFERENCES
        - TRIGGER
        - TRUNCATE
