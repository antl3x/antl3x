kind: TablePrivileges
metadata:
  name: totuna.private_tables.Playlist
spec:
  database: totuna
  schema: private_tables
  table: Playlist
  privileges:
    - role: postgres
      privileges:
        - TRIGGER
        - REFERENCES
        - TRUNCATE
        - DELETE
        - UPDATE
        - SELECT
        - INSERT
